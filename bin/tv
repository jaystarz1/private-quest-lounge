#!/bin/sh
# Mac operator control. Headsets must pair once with `bin/tv pair`.
# Default: start the authenticated screen feeder. `bin/tv stop` ends both tracks.
basedir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec node "$basedir/bin/tv-control" "${1:-start}"
